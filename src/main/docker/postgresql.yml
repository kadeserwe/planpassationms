version: '2'
services:
  planpassationms-postgresql:
    image: postgres:12.3
    # volumes:
    #     - ~/volumes/jhipster/planpassationms/postgresql/:/var/lib/postgresql/data/
    environment:
      - POSTGRES_USER=planpassationms
      - POSTGRES_PASSWORD=
      - POSTGRES_HOST_AUTH_METHOD=trust
    ports:
      - 5432:5432
